import React from "react";
import { Container } from "react-bootstrap";
import { Header } from "../components";

const Home = () => {
  return (
    <Container>
      <Header page="home" />
    </Container>
  );
};

export default Home;
